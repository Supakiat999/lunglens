/* =====================================================================
   LungLens — LIFF configuration
   Paste your LIFF ID from the LINE Developers Console here, e.g.:
     const LIFF_ID = "2001234567-Abcdefgh";
   Leave empty ("") to run in browser/demo mode (no LINE connection).
   The LIFF ID is public by design (it appears in every LIFF URL) —
   real secrets (channel secret / access token) belong on a server only.
   ===================================================================== */
const LIFF_ID = "2010756823-yiuPlaT0";
